import 'package:flutter/material.dart';

class WalkthroughView extends StatelessWidget {
  const WalkthroughView({Key? key}) : super(key: key);

  // TODO: Create a walkthrough view

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
